# Locked dependencies for XHS adapter integration boundary.
# Core adapter code is stdlib-only; these are pinned for external runner environments.
requests==2.32.3
websockets==12.0
playwright==1.48.0

