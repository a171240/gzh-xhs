# 内容生成控制台（桌面版，冻结兼容层）

`desktop-app` 当前仍保留在仓库中，但角色已经从“主定义源”降级为“兼容层 + 后续下线对象”。

## 当前定位
- 保留 Electron 桌面壳、旧 prompt/skill 配置和启动器
- 允许旧链路继续运行，避免误伤历史工作流
- 不再作为公众号主链的真相源
- XHS 与部分抓取桥接仍有残余耦合，后续单独脱钩

## 什么时候仍会用到
- 本地排查旧桌面工作流
- 兼容历史 skill/prompt 配置
- 追踪 XHS/抓取桥接里的旧入口依赖

## 什么时候不该再扩展
- 公众号内容、图片、排版、发布新逻辑
- repo-local skill 注册
- 新的上下文/规则真相源

## 快速启动（仅兼容排查）
```bash
cd 06-工具/desktop-app
npm install
npm run dev
```

## 下线节奏
- `freeze now`：保留目录和现有功能，不再扩展
- `remove later`：等 XHS/抓取桥接完成脱钩后，再清退旧入口
